const numeros = [16, 25, 144]

let indiceAleatorio = Math.floor(Math.random() * numeros.length)

let numeroSelecionado = numeros[indiceAleatorio]

let raiz = Math.sqrt(numeroSelecionado)

console.log(numeroSelecionado)
console.log(raiz)